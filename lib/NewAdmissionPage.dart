import 'package:flutter/material.dart';

class NewAdmissionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Admission Details'),
      ),
      body: Center(
        child: Text('New Admission Page'),
      ),
    );
  }
}
