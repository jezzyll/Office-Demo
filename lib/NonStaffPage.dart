import 'package:flutter/material.dart';

class NonStaffPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Non-Staff Details'),
      ),
      body: Center(
        child: Text('Non-Staff Page'),
      ),
    );
  }
}
